{
  "name": "drupal-module",
  "description": "Drupal Module for AdTechMedia.io",
  "keywords": [
    "drupal",
    "adtechmedia"
  ],
  "homepage": "https://github.com/AdTechMedia/drupal-module",
  "type": "library",
  "license": "MIT",
  "authors": [
    {
    "name": "DEEP Dev Team",
    "email": "hello@deep.mg",
    "website": "https://www.deep.mg"
    }
  ],
  "require": {
  },
  "require-dev": {
    "phpunit/phpunit": "3.7.*@stable",
    "ext-xdebug": "*",
    "tm/tooly-composer-script": "^1.0"
  },
  "scripts": {
    "preinstall": "bin/test/preinstall.sh",
    "install": "bin/test/install.sh ${TEST_SUITE}",
    "pretest": "bin/test/pretest.sh",
    "phpunit": "echo 'No test specified' && exit 0",
    "posttest": "bin/test/posttest.sh && npm run prepare-coverage",
    "prepare-coverage": "bin/test/prepare-coverage.sh ${TEST_SUITE}",
    "coverage": "bin/test/coverage.sh ${TEST_SUITE}",
    "remove-coverage": "bin/test/remove-coverage.sh"
  }
}
